export type Header = {
    account: string
    basket: string
    branches: string
    contactUs: string
    backToStore: string
    menu: string
    searchPlaceholder: string
    loggedInMessage: (name: string) => string
}
