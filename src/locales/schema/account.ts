export type Account = {
    account: string
    accountMenu: string
    addressBook: string
    dashboard: string
    orders: string
    profile: string
    signOut: string
    userMessage: (name: string) => string
    wishlist: string
}
